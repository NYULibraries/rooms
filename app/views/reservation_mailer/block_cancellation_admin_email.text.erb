A block has been created and some reservations cancelled. Please save this e-mail for your records:

===================================================

Block details

  Title: <%= @block.title%>
  Timeslot: <%= @block.start_dt.strftime('%a. %b %d, %Y %I:%M %p') %> -- <%= block.end_dt.strftime('%a. %b %d, %Y %I:%M %p') %>
  
===================================================

Cancelled reservations

<% if @cancel_request == "delete_with_alert" %>
(Note: Users HAVE been notified of these cancellations)
<% else %>
(Note: Users HAVE NOT been notified of these cancellations)
<% end %>

<%= formatted_reservations %>

===================================================